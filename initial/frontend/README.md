This is a [Next.js](https://nextjs.org/) project bootstrapped with Java Backend.

## Getting Started

First, run the development server:

```bash
yarn dev
```

First, run the development backend server:

```bash
mvn spring-boot:run
```
